class MapaLugarDetalle {
  String tipo;
  String name;
  String address;
  double lat;
  double lng;
  var rating;
  var price;

  MapaLugarDetalle(this.tipo, this.name, this.address, this.lat, this.lng,
      this.rating, this.price);
}
